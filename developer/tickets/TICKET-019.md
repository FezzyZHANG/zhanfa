# TICKET-019: 添加前端测试

| 属性     | 值                              |
| -------- | ------------------------------- |
| 优先级   | P2 - 中                         |
| 依赖     | 011                            |
| 创建日期 | 2026-05-05                      |

## 问题描述

后端测试覆盖率已从 61 提升至 188 个用例（TICKET-013），但前端代码完全没有测试。`frontend/` 目录下不存在任何项目级的 `.test.ts` 或 `.spec.ts` 文件。

前端技术栈已包含 vitest（`package.json` 中有配置），但无实际测试用例。

## 任务清单

- [x] 为核心工具函数编写单元测试（`lib/utils.ts` 等）
- [x] 为 API client 编写测试（Mock axios 响应）
- [x] 为关键组件编写渲染测试（K 线图表、自选股看板、策略列表等）
- [x] 配置 CI 中的前端测试步骤（GitHub Actions）
- [x] 在 `docs/frontend.md`（TICKET-017）中补充测试说明
