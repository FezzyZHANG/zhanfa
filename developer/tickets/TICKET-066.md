# TICKET-066: 关键用户场景测试与跨端契约持续维护

- **优先级:** P1 - 高
- **状态:** 🔁 持续维护（v066）
- **上次检查版本:** v066
- **依赖:** 009, 019, 032, 054

## 版本语义

本工单是持续维护的质量基线，不使用 `✅ 已完成` 作为常态状态。

- 版本格式为 `vNNN`，其中数字取最近一次完成检查时，工单系统内已纳入检查的最高 `TICKET-NNN` 编号。
- `v066` 表示本工单已检查截至 `TICKET-066` 的代码与测试体系，不表示下方所有长期改进项已经完成。
- 新增更高编号工单后，只有重新完成本工单定义的检查并记录结果，才推进版本；未检查时保留旧版本，让质量基线滞后可见。
- 若本工单将来被新的质量机制替代，应标记为 `❌ 取消` 并注明替代工单，而不是标记为一次性完成。

## 背景

当前测试体系以 Python 单元、service/API 集成测试和前端 Vitest 页面/组件测试为主。部分测试使用“E2E”命名，但实际只覆盖 FastAPI `TestClient` 的后端生命周期；前端页面测试通常会 mock hook 或 API client。CI 尚未启动真实前后端和浏览器，因此浏览器请求格式、后端真实响应、持久化副作用与最终 UI 呈现之间仍存在自动化盲区。

数据管理链路已经暴露出这类风险：同步批量刷新可能超过前端请求超时，后端返回的 `deferred` / `providers` 尚未进入前端展示契约，部分数据源提示文案也可能随 Provider 切换而过期。

## 长期目标

- 维护一组数量有限、面向用户可见行为的关键旅程测试。
- 以 FastAPI OpenAPI 为跨端 schema 来源，阻止请求或响应类型静默漂移。
- 浏览器场景使用真实 React、真实 HTTP API、真实 workflow 和隔离存储；只替换腾讯、AKShare 等不可控外部边界。
- 将稳定的离线场景作为 PR 门禁，将真实 Provider 探针作为定时检查，避免外部网络波动阻塞普通开发。
- 每次发现跨模块问题时，在能稳定复现问题的最低测试层补回归；只有关键用户链路风险才增加浏览器 E2E。

## 当前维护清单

### Critical

- [ ] 建立 OpenAPI schema 漂移检查，并让前端 API 类型可由契约生成或校验。
- [ ] 建立 Playwright 运行框架，可同时启动隔离 FastAPI 与真实前端。
- [ ] 覆盖数据管理主旅程：空状态 → 初始化 → 抓取 → 写入缓存 → 统计更新 → 页面刷新后仍可见。
- [ ] 覆盖数据刷新失败、部分失败和 `deferred > 0` 的用户可见反馈。
- [ ] 对齐数据刷新长任务语义、前端超时及 `RefreshResult` 展示字段。

### High

- [ ] 覆盖自选股创建、添加、删除和持久化的浏览器旅程。
- [ ] 覆盖回测提交、状态轮询和结果页展示的浏览器旅程。
- [ ] CI 失败时上传 Playwright trace、前后端日志和测试报告。
- [ ] 使用临时 SQLite、临时 parquet 目录和确定性 Fixture Provider，保证场景隔离、可重复。
- [ ] 增加少量真实腾讯 Provider 定时探针；该探针不作为普通 PR 的稳定门禁。

## 滚动检查步骤

每次推进版本时至少执行：

1. 确认当前最高工单编号，并检查自上次版本以来所有涉及前端、API、workflow、存储或外部 Provider 的改动。
2. 核对 `.github/workflows/ci.yml`、`developer/testing.md` 与实际命令一致。
3. 复核纳入范围内各工单的验证记录，并按 `developer/testing.md` 执行本次维护改动所需的 CI 对齐命令及已经落地的契约、用户旅程测试。
4. 检查关键用户旅程清单是否需要新增、合并或降级，避免 E2E 无限制增长。
5. 在下方记录检查结果，再同步本文件和 `developer/README.md` 中的 `vNNN`。

## 检查记录

| 版本 | 日期 | 纳入范围 | 结果 |
|------|------|----------|------|
| v066 | 2026-07-15 | TICKET-001～TICKET-066；现有 CI、测试目录、DataPage 数据刷新链路及本工单维护规则 | 已确认缺少浏览器级真实前后端 E2E 和自动契约门禁；初始改进项已列入本工单 |

### v066 验证记录

- 当前纯数字工单最高编号：`066`。
- `developer/README.md`、`developer/testing.md` 与本工单的持续维护版本均为 `v066`。
- `git diff --check` 通过；仅有工作区既有的 LF/CRLF 转换提示。
- 本次只修改开发者文档和工单系统，按 `developer/testing.md` 的纯文档规则未运行代码测试。

## 验收方式

本工单没有一次性的“全部完成”验收。每个 `vNNN` 检查点满足以下条件即可推进版本：

- 检查记录覆盖到当时最高工单编号。
- 已复核纳入工单的验证记录，并执行本次维护改动按 `developer/testing.md` 所要求的检查和已落地场景测试。
- 新发现的缺口已在本工单维护清单中记录，或拆为独立工单并建立链接。
- `developer/README.md`、`developer/testing.md` 和发布检查清单中的版本一致。
