# TICKET-018: 补充自动化模块文档

| 属性     | 值                              |
| -------- | ------------------------------- |
| 优先级   | P3 - 低                         |
| 依赖     | 014                            |
| 创建日期 | 2026-05-05                      |

## 问题描述

`docs/` 目录下没有自动化模块的任何文档。`docs/architecture.md` 中仅有一句话提及 `automation/`（"定时调度器 + 工作流"），但缺少：

1. **调度器架构** — `scheduler.py` 使用的调度库、作业配置方式、启动方式
2. **工作流说明** — `update_daily_data()` 和 `weekly_index_rebalance()` 的触发条件、执行逻辑、当前限制
3. **自动化配置** — 如何添加新作业、如何配置调度间隔
4. **当前局限性** — 调度器无持久化存储（重启丢失）、无错误通知机制、工作流仅为基础骨架代码

这些信息对运维和后续开发至关重要，但目前在文档中完全缺失。

## 任务清单

- [x] 创建 `docs/automation.md`，包含调度器架构、工作流列表、配置方法
- [x] 诚实记录当前限制（无持久化、无通知、工作流为骨架代码）
- [x] 说明如何添加新的定时作业
- [x] 在 `docs/index.md` 的文档导航表中添加 `automation.md` 链接
- [x] 在 `docs/architecture.md` 中补充交叉引用
